import styles from "./Footer.module.scss";

const Footer = () => {
    return (
        <footer className={styles.footer}>
            <span>DM</span>
            <span>@ 2022</span>
        </footer>
    );
};

export default Footer;
